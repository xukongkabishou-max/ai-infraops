# Backend Admin Web

后端 RBAC 管理页面，默认运行在 `3001` 端口。

当前是静态 demo，用于后续对接 `services/backend` 的登录、用户、角色、权限和菜单 API。

## 本地运行

在仓库根目录运行：

```powershell
npm run dev:backend-admin
```

访问：

```text
http://localhost:3001
```
